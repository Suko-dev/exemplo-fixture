import test, { describe } from "node:test";
import { ClienteService } from "./cliente-service";

describe('ClienteService', () => {
    test('dale', () => {
        const clienteService = new ClienteService({ salvar(cliente) {
            
        },: () => { } })
    })
})